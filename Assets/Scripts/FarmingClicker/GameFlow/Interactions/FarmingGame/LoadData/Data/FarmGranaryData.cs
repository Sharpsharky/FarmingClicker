using InfiniteValue;

namespace FarmingClicker.GameFlow.Interactions.FarmingGame.LoadData.Data
{
    public class FarmGranaryData : WorkPlaceData
    {
        public FarmGranaryData(int upgradeLevel, InfVal currentCurrency) : base(upgradeLevel, currentCurrency)
        {
        }
    }
}