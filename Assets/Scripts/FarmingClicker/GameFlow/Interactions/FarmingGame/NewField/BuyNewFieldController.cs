using UnityEngine;

namespace FarmingClicker.GameFlow.Interactions.FarmingGame.NewField
{
    public class BuyNewFieldController : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
