using System;

namespace FarmingClicker.GameFlow.Interactions.FarmingGame.Worker
{
    public enum StatisticsTypes 
    {
        LEVEL = 0, 
        NUMBER_OF_WORKERS = 1, 
        CROPPED_CURRENCY = 2, 
        WORKING_SPEED = 3, 
        MOVING_SPEED = 4, 
        MAX_LOAD = 5
    }
}